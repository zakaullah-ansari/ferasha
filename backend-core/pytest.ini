[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
testpaths = tests apps
addopts =
    --strict-markers
    --disable-warnings
    -ra
markers =
    django_db: test requires database access
    unit: pure-Python test with no Django dependency
filterwarnings =
    error::DeprecationWarning:apps.*
